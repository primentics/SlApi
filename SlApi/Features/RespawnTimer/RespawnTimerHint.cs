namespace SlApi.Features.RespawnTimer
{
    public class RespawnTimerHint
    {
        public int Chance { get; set; } = 100;
        public string Hint { get; set; } = "<color=red>SCPs</color> can talk to other players via the <color=green>Proximity</color> chat by using the <color=red>.popvoice</color> command!";
    }
}